spring:
  profiles:
    active: local
  datasource:
    url: jdbc:postgresql://localhost:5432/smes
    username: suser
    password: smes
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
    #      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.sql: debug